#!/bin/bash
#Creates the links to the aperturecomputemodule include directories

version="trial_1f"

ln -s ../../../../aperturecomputemodule/${version}/${version}/src/logger .
ln -s ../../../../aperturecomputemodule/${version}/${version}/src/shapefunction .
ln -s ../../../../aperturecomputemodule/${version}/${version}/src/udp .
